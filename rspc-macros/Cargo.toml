[package]
name = "rspc-macros"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
proc-macro = true

rspc = { path = "../rpsc" }

[dependencies]
syn = "1"
quote = "1"
